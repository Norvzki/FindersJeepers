@using MudBlazor

<MudCard Elevation="2" Style="border-radius:14px;">
    <MudCardHeader>
        <CardHeaderContent>
            <MudText Typo="Typo.h6" Class="fw-bold">Trip History</MudText>
        </CardHeaderContent>
    </MudCardHeader>
    <MudDivider />
    <MudCardContent>
        @if (!Trips.Any())
        {
            <MudAlert Severity="Severity.Info" Dense="true">No completed trips recorded yet.</MudAlert>
        }
        else
        {
            <MudTable T="TripSummary" Items="Trips" Dense="true" Hover="true" Elevation="0">
                <HeaderContent>
                    <MudTh>Trip ID</MudTh>
                    <MudTh>Jeep</MudTh>
                    <MudTh>Route</MudTh>
                    <MudTh>Departed</MudTh>
                    <MudTh>Arrived</MudTh>
                    <MudTh></MudTh>
                </HeaderContent>
                <RowTemplate>
                    <MudTd>#@context.Id</MudTd>
                    <MudTd>@(context.JeepneyPlateNumber ?? "—")</MudTd>
                    <MudTd>@context.RouteCode</MudTd>
                    <MudTd>@(context.DepartureTime?.ToString("MMM dd, hh:mm tt") ?? "—")</MudTd>
                    <MudTd>@(context.ArrivalTime?.ToString("MMM dd, hh:mm tt") ?? "—")</MudTd>
                    <MudTd>
                        <MudIconButton Icon="@Icons.Material.Filled.OpenInNew" Size="Size.Small"
                                       Href="@($"/trips/{context.Id}")" />
                    </MudTd>
                </RowTemplate>
            </MudTable>
        }
    </MudCardContent>
</MudCard>

@code {
    [Parameter, EditorRequired]
    public IEnumerable<TripSummary> Trips { get; set; } = Enumerable.Empty<TripSummary>();
}